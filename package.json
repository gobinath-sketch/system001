{
    "name": "gkterp-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run server\" \"npm run client\" --kill-others --prefix-colors auto",
        "server": "cd server && npm run dev",
        "client": "cd client && npm run dev",
        "install-all": "cd server && npm install && cd ../client && npm install",
        "audit": "npm audit && node scripts/generate-audit-log.js",
        "audit:log": "node scripts/generate-audit-log.js",
        "audit:full": "npm run audit && npm --prefix client audit && npm --prefix server audit && node scripts/generate-audit-log.js",
        "audit:single": "powershell -ExecutionPolicy Bypass -File scripts/full-audit-log.ps1"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
