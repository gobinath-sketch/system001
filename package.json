{
    "name": "gkterp-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run server\" \"npm run client\" --kill-others --prefix-colors auto",
        "server": "cd server && npm run dev",
        "client": "cd client && npm run dev",
        "install-all": "cd server && npm install && cd ../client && npm install",
        "audit:log": "node scripts/generate-audit-log.js"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
